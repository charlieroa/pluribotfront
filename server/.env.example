# ─── Server ───
PORT=3002
NODE_ENV=development

# ─── Authentication ───
JWT_SECRET=change-me-to-a-random-string

# ─── Database ───
# SQLite (default for development):
DATABASE_URL="file:./dev.db"
# PostgreSQL (production):
# DATABASE_URL="postgresql://user:password@localhost:5432/pluribots?schema=public"

# ─── LLM Providers ───
ANTHROPIC_API_KEY=sk-ant-...
OPENAI_API_KEY=sk-...
GOOGLE_API_KEY=AIza...

# ─── Image Generation ───
APIFRAME_API_KEY=
UNSPLASH_ACCESS_KEY=

# ─── CORS ───
# Comma-separated origins allowed to access the API.
# Default: http://localhost:5173 (Vite dev server)
CORS_ORIGINS=http://localhost:5173

# ─── File Storage ───
# "local" (default) writes to disk under server/uploads/
# "s3" uses AWS S3 (requires the keys below)
STORAGE_PROVIDER=local

S3_BUCKET=
S3_REGION=
AWS_ACCESS_KEY_ID=
AWS_SECRET_ACCESS_KEY=

# Base URL prepended to uploaded file paths (leave empty for local)
CDN_BASE_URL=

# ─── Deploy Cloud ───
NETLIFY_TOKEN=          # Personal access token de Netlify para deploy cloud
